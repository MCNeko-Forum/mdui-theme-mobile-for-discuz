# MDUI 手机版 DIY 模块

手机版门户首页（`touch/portal/index.htm`）的内容块都可以用 Discuz 原生 DIY 模块接管。
数据层在 [portal_index.php](touch/php/portal_index.php)。

## 怎么配

后台 → 门户 → **专题管理** → 建/编辑专题 → 进入专题页点 **DIY** → 添加模块 →
在模块设置里把 **模块名称** 填成下表的值，保存并「提交」DIY。

> 匹配只认「模块名称」（`common_block.name`）。`blockclass` 是系统按 block 类目录自动生成的
> （`forum_thread` / `group_thread` 等），后台改不了，所以不能用它来认模块。
> 模块名重复时取 `bid` 最大（最新建）的那个。

模块放在哪个专题、哪个位置都无所谓，手机版首页只按名字取数据，不读排版。

## 模块一览

| 模块名称 | 首页位置 | 用到的字段 | 没配 DIY 时 |
| --- | --- | --- | --- |
| `MDUI手机版首页导航` | 标题栏正下方，横向可滚动的胶囊导航 | 见下方说明 | 整块不显示 |
| `MDUI手机版幻灯片` | 导航下方横向吸附滚动图 | 链接、标题、图片 | 整块不显示 |
| `MDUI手机版头条` | 幻灯片下方红喇叭卡片，多条时逐条上滚循环 | 链接、标题 | 整块不显示 |
| `MDUI手机版宫格入口` | 头条下方一行等分格子 | 见下方说明 | 整块不显示 |
| `MDUI手机版最新回复` | 选项卡「最新回复」 | 链接、标题、作者、时间 | 回落到论坛最新回复 10 条 |
| `MDUI手机版热帖` | 选项卡「热帖」 | 链接、标题、作者、查看数 | 回落到论坛热帖 10 条 |
| `MDUI手机版精华帖子` | 选项卡「精华帖子」 | 链接、标题、作者、时间 | 回落到论坛精华 10 条 |
| `MDUI手机版最新` | 下方信息流（整卡可点） | 链接、标题、图片、作者、时间、查看数、回复数 | 回落到论坛最新发表 20 条 |

导航和宫格都要选 **展示类 → 静态模块 → 自定义HTML**。HTML 类模块的渲染结果官方在保存时
就写进了 `common_block.summary`（`function_block.php:263`），所以数据层直接读 `summary`，
不走 `itemlist`。

- **首页导航**：内容写一串 `<a href="...">文字</a>`，包在 `<ul><li>` 里也行。
  每个链接渲染成 MDUI 胶囊（`.pt_nav a`），超出一屏横滑
- **宫格入口**：每个 `<a>` 里放 `<h2>` 标题 + `<span>` 说明，例如

  ```html
  <a href="plugin.php?id=zqlj_sign&amp;mobile=2"><h2 style="color:#ff9900">每日签到</h2><span>每日领硬币</span></a>
  ```

  有几个 `<a>` 就等分几格（`.pt_entry` 用 flex，不因文字长短变宽窄），`<h2>` 上的
  内联颜色会保留

头条多于一条时纯 CSS 循环轮播（每条停 3 秒，滚到末条后无缝回到第一条）。
`steps()` 的步数不接受 `var()`，所以时长和 timing-function 由模板按条数算好写内联；
系统开了「减少动效」时自动不滚。

公告区（喇叭卡片下方）读的是后台 → 全局 → **站点公告**，不走 DIY。

## 字段来源

- 链接 / 标题 / 图片 = DIY 条目自身的 `url` / `title` / `pic`（本地附件、远程附件、外链三种都支持，
  勾了生成缩略图就用缩略图）
- 作者 / 时间 / 查看数 / 回复数 = DIY 模块的**扩展字段**。在模块设置的「字段」里勾上
  作者、发表时间、查看数、回复数，前台才有值；没勾就留空
- 信息流的多图 = 帖内附件图前三张，**只在条目自己没有图片时**才去取。用的是帖子 tid，
  所以模块的数据来源要是帖子类（`idtype = tid`），非帖子来源的条目取不到多图

## 注意

- 幻灯片和头条**没有回落**，不配 DIY 就整块不显示，首页不会留白框
- 选项卡和信息流一旦配了 DIY 且有条目，就完全用 DIY 的内容，不再混论坛帖子
- 模块条目数由 DIY 模块自身的「显示数量」决定，模板不截断；信息流是每点一次「更多」多显示 5 条
- 首页命名模块中，数据源为论坛主题 / 群组主题（`forum_thread` / `group_thread`）的模块会在每次访问首页时调用 Discuz 原生更新函数，帖子数据实时刷新；静态 HTML 导航、宫格和其他非帖子模块仍按原缓存策略运行。
